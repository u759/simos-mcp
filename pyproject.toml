[project]
name = "simosmcp"
version = "1.0.0"
description = "MCP server for Simos 18.1 ECU tuning — read, write, diff, and validate XDF/BIN files"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
simosmcp = "server:main"
